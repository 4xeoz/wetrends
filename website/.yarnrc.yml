# Yarn 4 defaults to Plug'n'Play, which resolves modules without a
# node_modules directory. Next.js, Prisma and next/font all expect real files on
# disk, so this project uses the classic linker instead. Removing this line will
# break `prisma generate` and the Next build.
nodeLinker: node-modules
